conf:
  undefined_columns: "keep"
  on_duplicate_rows:
    tactic: "keep"

columns:
  salary:
    dtype: float
    constraints:
      min_policy:
        threshold: 0
        violate_action:
          tactic: "fail" # Fails entirely if ANY record has negative salary
