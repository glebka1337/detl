conf:
  undefined_columns: "drop"
  on_duplicate_rows:
    tactic: "keep"

columns:
  role:
    dtype: string
    on_null:
      tactic: "fill_value" 
      # ERROR: Attempting to use fill_value without actually 
      # providing the 'value: "guest"' property will trigger Pydantic block.
