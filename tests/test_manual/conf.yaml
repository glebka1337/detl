conf:
  undefined_columns: "keep"
  on_duplicate_rows: "keep"
  defaults:
    float:
      on_null:
        tactic: "fill_median"

columns:
  Flight_Date:
    
    dtype: date
    format:
      input: "%Y-%m-%d"
      output: "%d.%m.%Y"
    
    on_null:
      tactic: "fil_value"
      value: 1234
